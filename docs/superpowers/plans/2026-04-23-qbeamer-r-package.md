# qbeamer R Package Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the qbeamer Quarto Beamer template into a fully functional R package with RStudio integration.

**Architecture:** Quarto extension files bundled inside `inst/extdata/_extensions/qbeamer/`. R functions provide installation, rendering, and previewing. RStudio project and document templates integrate with the IDE. NAMESPACE auto-generated by roxygen2.

**Tech Stack:** R, roxygen2, fs, quarto (Suggests), LaTeX/Beamer

---

## File Structure

| File | Responsibility |
|------|---------------|
| `DESCRIPTION` | Package metadata, dependencies |
| `NAMESPACE` | Auto-generated by roxygen2 |
| `LICENSE` | MIT license year + holder |
| `LICENSE.md` | Full MIT license text |
| `.Rbuildignore` | Exclude non-package files from build |
| `R/install_extension.R` | `install_extension()` — copies extension into user project |
| `R/create_project.R` | `create_project()` — RStudio project template binding |
| `R/render.R` | `qbeamer_render()` and `qbeamer_preview()` wrappers |
| `R/zzz.R` | `.onAttach()` startup message |
| `inst/extdata/_extensions/qbeamer/_extension.yml` | Quarto extension format definition |
| `inst/extdata/_extensions/qbeamer/preamble.tex` | LaTeX preamble (renamed from before-body.tex, lipsum removed) |
| `inst/extdata/_extensions/qbeamer/title.tex` | Beamer title slide template partial |
| `inst/rstudio/templates/project/qbeamer.dcf` | RStudio project template config |
| `inst/rstudio/templates/project/skeleton/skeleton.qmd` | Project skeleton QMD |
| `inst/rmarkdown/templates/qbeamer-presentation/template.yaml` | RStudio document template config |
| `inst/rmarkdown/templates/qbeamer-presentation/skeleton/skeleton.qmd` | Document skeleton QMD |
| `inst/examples/example.qmd` | Full E-M algorithm demo |

---

### Task 1: Package Scaffolding

**Files:**
- Create: `DESCRIPTION`
- Create: `LICENSE`
- Create: `LICENSE.md`
- Create: `.Rbuildignore`

- [ ] **Step 1: Create DESCRIPTION**

```
Package: qbeamer
Title: Custom Quarto Beamer Presentation Template
Version: 0.1.0
Authors@R:
    person("Gabriel", "Cabrera Guzmán",
           email = "gc@abrerag.rbind.io",
           role = c("aut", "cre"),
           comment = c(ORCID = "YOUR-ORCID-ID"))
Description: Provides a Quarto extension and RStudio integration for creating
    professional Beamer presentations. Includes custom styling, enhanced
    author/affiliation support with ORCID, and helper functions for rendering
    and previewing presentations.
License: MIT + file LICENSE
Encoding: UTF-8
Roxygen: list(markdown = TRUE)
Imports: fs
Suggests: quarto
URL: https://github.com/gcabrerag/qbeamer
BugReports: https://github.com/gcabrerag/qbeamer/issues
```

- [ ] **Step 2: Create LICENSE**

```
YEAR: 2026
COPYRIGHT HOLDER: Gabriel Cabrera Guzmán
```

- [ ] **Step 3: Create LICENSE.md**

Full MIT license text with 2026 and Gabriel Cabrera Guzmán.

- [ ] **Step 4: Create .gitignore**

```
.Rhistory
.RData
.Rproj.user
```

- [ ] **Step 5: Create .Rbuildignore**

```
^docs$
^slide-r\.Rproj$
^example\.qmd$
^example\.pdf$
^example\.tex$
^fig$
^README\.md$
^CLAUDE\.md$
^\.DS_Store$
^.*\.Rproj$
^\.Rproj\.user$
```

- [ ] **Step 6: Commit**

```bash
git add DESCRIPTION LICENSE LICENSE.md .Rbuildignore .gitignore
git commit -m "feat: add R package scaffolding (DESCRIPTION, LICENSE, .Rbuildignore)"
```

Note: `RoxygenNote` field will be auto-set by `devtools::document()` in Task 10.

---

### Task 2: Quarto Extension Files

**Files:**
- Create: `inst/extdata/_extensions/qbeamer/_extension.yml`
- Create: `inst/extdata/_extensions/qbeamer/preamble.tex` (from `before-body.tex`, remove `\usepackage{lipsum}`)
- Copy: `inst/extdata/_extensions/qbeamer/title.tex` (from `title.tex`)

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p inst/extdata/_extensions/qbeamer
```

- [ ] **Step 2: Create `_extension.yml`**

```yaml
title: qbeamer
author: Gabriel Cabrera Guzmán
version: 0.1.0
contributes:
  formats:
    beamer:
      include-in-header:
        - preamble.tex
      template-partials:
        - title.tex
      pdf-engine: xelatex
      navigation: horizontal
      theme: default
      colortheme: default
      aspectratio: 43
      keep-tex: true
```

- [ ] **Step 3: Create `preamble.tex`**

Copy contents of `before-body.tex` into `inst/extdata/_extensions/qbeamer/preamble.tex`. Remove line 30 (`\usepackage{lipsum}`).

- [ ] **Step 4: Copy `title.tex`**

Copy `title.tex` to `inst/extdata/_extensions/qbeamer/title.tex` unchanged.

- [ ] **Step 5: Commit**

```bash
git add inst/extdata/_extensions/qbeamer/
git commit -m "feat: add Quarto extension files (_extension.yml, preamble.tex, title.tex)"
```

---

### Task 3: R Function — `install_extension()`

**Files:**
- Create: `R/install_extension.R`

- [ ] **Step 1: Write `install_extension.R`**

```r
#' Install the qbeamer Quarto extension
#'
#' Copies the qbeamer Quarto extension files into a project directory,
#' making the `qbeamer-beamer` format available for use.
#'
#' @param path Path to the project directory. Defaults to the current
#'   working directory.
#' @param overwrite If `TRUE`, overwrite existing extension files.
#'   Defaults to `FALSE`.
#'
#' @return Invisibly returns the path to the installed extension directory.
#' @export
install_extension <- function(path = ".", overwrite = FALSE) {
  target <- fs::path(path, "_extensions", "qbeamer")

  if (fs::dir_exists(target) && !overwrite) {
    message("qbeamer extension already installed at '", target, "'. ",
            "Use overwrite = TRUE to reinstall.")
    return(invisible(target))
  }

  source <- system.file("extdata", "_extensions", "qbeamer",
                         package = "qbeamer", mustWork = TRUE)

  fs::dir_create(fs::path(path, "_extensions"))
  fs::dir_copy(source, target, overwrite = overwrite)

  message("qbeamer extension installed to '", target, "'.")
  invisible(target)
}
```

- [ ] **Step 2: Commit**

```bash
git add R/install_extension.R
git commit -m "feat: add install_extension() function"
```

---

### Task 4: R Function — `create_project()`

**Files:**
- Create: `R/create_project.R`

- [ ] **Step 1: Write `create_project.R`**

```r
#' Create a new qbeamer presentation project
#'
#' RStudio project template binding. Called by RStudio when a user creates
#' a new project from the qbeamer template. Can also be called directly.
#'
#' @param path Path to the new project directory.
#' @param title Presentation title. Used by RStudio project wizard.
#' @param ... Additional arguments passed by RStudio (ignored).
#'
#' @return Invisibly returns the project path.
#' @export
create_project <- function(path, title = "Untitled Presentation", ...) {
  fs::dir_create(path)

  # Copy skeleton QMD and substitute title
  skeleton <- system.file("rstudio", "templates", "project", "skeleton",
                           "skeleton.qmd", package = "qbeamer", mustWork = TRUE)
  content <- readLines(skeleton, encoding = "UTF-8")
  content <- gsub("Untitled Presentation", title, content, fixed = TRUE)
  writeLines(content, fs::path(path, "skeleton.qmd"), useBytes = TRUE)

  # Install the Quarto extension
  install_extension(path = path)

  invisible(path)
}
```

- [ ] **Step 2: Commit**

```bash
git add R/create_project.R
git commit -m "feat: add create_project() RStudio binding"
```

---

### Task 5: R Functions — `qbeamer_render()` and `qbeamer_preview()`

**Files:**
- Create: `R/render.R`

- [ ] **Step 1: Write `render.R`**

```r
#' Render a qbeamer presentation
#'
#' Wrapper around [quarto::quarto_render()] that checks the qbeamer extension
#' is installed before rendering.
#'
#' @param input Path to the `.qmd` file to render.
#' @param ... Additional arguments passed to [quarto::quarto_render()].
#'
#' @return The return value of [quarto::quarto_render()].
#' @export
qbeamer_render <- function(input, ...) {
  check_extension(input)
  if (!requireNamespace("quarto", quietly = TRUE)) {
    stop("Package 'quarto' is required. Install it with install.packages('quarto').",
         call. = FALSE)
  }
  quarto::quarto_render(input, ...)
}

#' Preview a qbeamer presentation
#'
#' Wrapper around [quarto::quarto_preview()] that checks the qbeamer extension
#' is installed before previewing.
#'
#' @param input Path to the `.qmd` file to preview.
#' @param ... Additional arguments passed to [quarto::quarto_preview()].
#'
#' @return The return value of [quarto::quarto_preview()].
#' @export
qbeamer_preview <- function(input, ...) {
  check_extension(input)
  if (!requireNamespace("quarto", quietly = TRUE)) {
    stop("Package 'quarto' is required. Install it with install.packages('quarto').",
         call. = FALSE)
  }
  quarto::quarto_preview(input, ...)
}

#' Check that the qbeamer extension is installed
#'
#' @param input Path to a `.qmd` file. The extension is checked relative to
#'   the file's parent directory.
#' @return `TRUE` invisibly if found; emits a message if not.
#' @noRd
check_extension <- function(input) {
  project_dir <- fs::path_dir(fs::path_abs(input))
  ext_dir <- fs::path(project_dir, "_extensions", "qbeamer")
  if (!fs::dir_exists(ext_dir)) {
    stop("qbeamer extension not found in '", project_dir, "'. ",
         "Run qbeamer::install_extension('", project_dir, "') first.",
         call. = FALSE)
  }
  invisible(TRUE)
}
```

- [ ] **Step 2: Commit**

```bash
git add R/render.R
git commit -m "feat: add qbeamer_render() and qbeamer_preview() helpers"
```

---

### Task 6: R Function — `.onAttach()`

**Files:**
- Create: `R/zzz.R`

- [ ] **Step 1: Write `zzz.R`**

```r
.onAttach <- function(libname, pkgname) {
  packageStartupMessage("qbeamer v", utils::packageVersion("qbeamer"))
}
```

- [ ] **Step 2: Commit**

```bash
git add R/zzz.R
git commit -m "feat: add .onAttach startup message"
```

---

### Task 7: RStudio Project Template

**Files:**
- Create: `inst/rstudio/templates/project/qbeamer.dcf`
- Create: `inst/rstudio/templates/project/skeleton/skeleton.qmd`

- [ ] **Step 1: Create directories**

```bash
mkdir -p inst/rstudio/templates/project/skeleton
```

- [ ] **Step 2: Create `qbeamer.dcf`**

```dcf
Binding: create_project
Title: Quarto Beamer Presentation (qbeamer)
OpenFiles: skeleton.qmd

Parameter: title
Widget: TextInput
Label: Presentation Title
Default: Untitled Presentation
```

- [ ] **Step 3: Create project `skeleton.qmd`**

```yaml
---
title: "Untitled Presentation"
subtitle: ""
author:
  - name: "Author Name"
    orcid: 0000-0000-0000-0000
    affiliations:
      - ref: inst1

affiliations:
  - id: inst1
    name: Institution Name
    department: Department Name

date: "today"
date-format: long

format:
  qbeamer-beamer: default
---

## First Slide

Content here.
```

- [ ] **Step 4: Commit**

```bash
git add inst/rstudio/templates/project/
git commit -m "feat: add RStudio project template"
```

---

### Task 8: RStudio Document Template

**Files:**
- Create: `inst/rmarkdown/templates/qbeamer-presentation/template.yaml`
- Create: `inst/rmarkdown/templates/qbeamer-presentation/skeleton/skeleton.qmd`

- [ ] **Step 1: Create directories**

```bash
mkdir -p inst/rmarkdown/templates/qbeamer-presentation/skeleton
```

- [ ] **Step 2: Create `template.yaml`**

```yaml
name: Quarto Beamer (qbeamer)
description: >
  A Quarto Beamer presentation using the qbeamer template with custom styling,
  ORCID support, and professional formatting. Run qbeamer::install_extension()
  after creating the file to install the extension.
create_dir: false
```

- [ ] **Step 3: Create document `skeleton.qmd`**

Same content as the project skeleton in Task 7, Step 3.

- [ ] **Step 4: Commit**

```bash
git add inst/rmarkdown/templates/qbeamer-presentation/
git commit -m "feat: add RStudio document template"
```

---

### Task 9: Example File

**Files:**
- Create: `inst/examples/example.qmd` (adapted from current `example.qmd`)

- [ ] **Step 1: Create directory**

```bash
mkdir -p inst/examples
```

- [ ] **Step 2: Copy figures**

Copy the `fig/` directory to `inst/examples/fig/` so that the example's figure references (`fig/rstudio-qmd-how-it-works.png`, `fig/gwdp.pdf`) remain valid.

```bash
cp -r fig inst/examples/fig
```

- [ ] **Step 3: Copy and adapt `example.qmd`**

Copy the current `example.qmd` to `inst/examples/example.qmd`. Update the YAML to use `qbeamer-beamer` format instead of plain `beamer`:

```yaml
format:
  qbeamer-beamer: default
```

Remove the `include-in-header` and `template-partials` keys (these are now handled by the extension).

- [ ] **Step 4: Commit**

```bash
git add inst/examples/
git commit -m "feat: add example presentation in inst/examples/"
```

---

### Task 10: Generate NAMESPACE and Documentation

**Files:**
- Generated: `NAMESPACE`
- Generated: `man/*.Rd`

- [ ] **Step 1: Run roxygen2**

```r
devtools::document()
```

Expected: creates `NAMESPACE` with `export(create_project)`, `export(install_extension)`, `export(qbeamer_preview)`, `export(qbeamer_render)`, and generates `.Rd` files in `man/`.

- [ ] **Step 2: Commit**

```bash
git add NAMESPACE man/
git commit -m "docs: generate NAMESPACE and roxygen documentation"
```

---

### Task 11: Verify Package

- [ ] **Step 1: Run R CMD check**

```r
devtools::check()
```

Expected: 0 errors, 0 warnings. Notes about `quarto` not being available are acceptable.

- [ ] **Step 2: Test install_extension manually**

```r
# In a temp directory
tmp <- tempdir()
qbeamer::install_extension(tmp)
# Verify _extensions/qbeamer/ exists with 3 files
list.files(file.path(tmp, "_extensions", "qbeamer"))
```

Expected: `_extension.yml`, `preamble.tex`, `title.tex`

- [ ] **Step 3: Fix any issues found**

- [ ] **Step 4: Final commit if needed**

---

### Task 12: Update CLAUDE.md

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Update CLAUDE.md to reflect R package structure**

Update build commands, architecture description, and add R package development commands (`devtools::check()`, `devtools::document()`, `devtools::install()`).

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md for R package structure"
```
